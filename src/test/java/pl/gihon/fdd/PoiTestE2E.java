package pl.gihon.fdd;

import org.junit.Test;

public class PoiTestE2E {

	@Test
	public void app_givenAllInputs_producesAreas() {

		// inputFileStream = readFileFromCsv();
		// List<Place> places = csvMapper.map(inputFileStream)
		// validator.validatePlaces(places);
		// Map<Place, Geo> cache = loadCache.load(places)
		// List<LocalisedPlace> localised = googleLocalisator.localise(places,
		// cache)
		// storageService.store(localised);
		// sessionManager.storeInSession() ; //web flow
		//
		//
		// List<SimpleArea> simpleAreas = mapPlacesToAreas(places) // virtual,
		// job done by user
		//
		//
		// areaVerificator.verify(areas, places) // check if no place is missing
		// in areas, if nothing is dubled
		// List<Area> areas = resolveFullAreas(simpleAreas, localised);

	}

}
